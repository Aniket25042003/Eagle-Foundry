import { describe, it } from 'vitest';

describe.skip('Projects Integration', () => {
  it('allows a company admin to create and publish a project', async () => {
    // Integration flow requires test database + seeded users/org.
  });

  it('returns only published projects on the public listing', async () => {
    // Integration flow requires test database + seeded users/org.
  });
});
